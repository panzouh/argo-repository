pre-commit:
  scripts:
    echo-test:
      skip_empty: false
      run: echo "{staged_files}"
    helm-docs:
      skip_empty: false
      runner: docker run --rm --volume "$(pwd):/helm-docs"  jnorwood/helm-docs:latest -l trace