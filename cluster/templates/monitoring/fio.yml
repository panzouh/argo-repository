{{- define "fio.enabled" -}}
{{- and .Values.monitoring.fio.enabled .Values.monitoring.enabled  -}}
{{- end -}}
{{ if eq (include "fio.enabled" .) "true" }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fio-exporter
  namespace: {{ .Values.argocd.namespace }}
  finalizers:
  {{- include "argocd.applications.finalizers" . | nindent 4 }}
spec:
  {{- template "cluster.syncPolicy" . }}
  destination:
    namespace: {{ .Values.olm.namespace }}
    server: https://kubernetes.default.svc
  project: default
  source:
    path: {{ .Values.fio.chart.path }}
    repoURL: {{ .Values.fio.chart.repo }}
    targetRevision: {{ .Values.fio.chart.targetRevision }}
    helm:
      version: v3
      values: |-
        {{- if and .Values.monitoring.enabled .Values.monitoring.prometheus.enabled }}
        prometheusScrape:
          enabled: true
        {{- end }}
{{ end }}
